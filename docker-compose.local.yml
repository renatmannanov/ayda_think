services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: ayda_think
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: localpass
    ports:
      - "5433:5432"
    volumes:
      - pgdata_local:/var/lib/postgresql/data

volumes:
  pgdata_local:
