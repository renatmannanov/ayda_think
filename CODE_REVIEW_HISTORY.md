# История Код-Ревью (Code Review History)

Этот документ отслеживает историю анализа кода, архитектурных решений и работу над техническим долгом.

## Версия 1.0.0 (28.11.2024)
**Статус:** Пост-релизный анализ
**Ревьюер:** Antigravity (AI Agent)

### 1. Запрос пользователя
> теперь давай проведем код ревью.
> Я хочу чтобы ты собрал мне аналитику по нашей кодовой базе.
> Не торопись, подробно пройдись по каждому файлу
>
> Что хочу понимать:
> 1. список файлоф + строки кода + тезисно какую задачу выполняет
> 2. хорошие практики в кодовой базе + 1-2 примера
> 3. плохие практики в кодовой базе + 1-2 примера
> 4. какие файлы тебе тяжело обновлять и есть риски его полностью сломать.
> 5. твои предложения по оптимизации, для того, чтобы тебе было легко ориентироваться по кодовой базе, работать быстрее и делать меньше ошибок и поломок.
> 6. какие правила можем добавить в наши файлы настрок PROMPT.md и AI_SETTINGS.md

### 2. Анализ и Ответ Агента
> Вот подробный анализ вашего проекта **Ayda Think**.
>
> **1. Структура и назначение файлов**
> Всего в проекте **~1800 строк кода**.
> *   `api_server.py` (150 строк): **Backend API**. Критический файл.
> *   `bot/handlers.py` (280 строк): **Логика бота**. Самый сложный файл.
> *   `webapp/app.js` (300 строк): **Логика Mini App**.
> *   ... (полный список в чате)
>
> **2. Хорошие практики 👍**
> *   **Абстракция хранилища (`BaseStorage`)**: Легко сменить базу данных.
> *   **Fail-Fast конфигурация**: Проверка токенов при старте.
>
> **3. Плохие практики и Риски ⚠️**
> *   **Эфемерное хранилище (`users.json`)**: Потеря данных при деплое на Render.
> *   **Магические числа**: Использование индексов `row[10]` вместо констант.
> *   **Монолитный Frontend**: `app.js` делает всё сразу.
>
> **4. Хрупкие файлы 🥚**
> *   `api_server.py`: Ошибка роняет весь сервис.
> *   `bot/handlers.py`: Перегружен логикой ("Божественный объект").
>
> **5. Предложения по оптимизации 🚀**
> *   Переезд на БД (PostgreSQL/SQLite).
> *   Константы для колонок Google Sheets.
> *   Разделение `app.js` на слои (API, UI, Logic).

### 3. Обсуждение и Уточнения
**Вопрос пользователя:**
> 1. "Риск: Каждый раз после обновления кода все пользователи "теряются"..." Что значит "после обновления кода"? любой новый коммит в гит?
> 2. "Монолитный Frontend". Какие рекомендации по исправлению монолита фронта?
> 3. "Хрупкие файлы" Какие рекомендации по исправлению этих двуух файлов?
> 4. Как можно оптимизировать файл webapp/styles.css, чтобы ты меньше его ломал?

**Ответ Агента:**
> 1. **Про обновление:** Да, Render использует эфемерную файловую систему. `git push` = чистый сервер = удаление `users.json`.
> 2. **Про Frontend:** Разбить `app.js` на `api.js` (запросы), `ui.js` (отрисовка), `main.js` (логика).
> 3. **Про Backend:** Вынести модели в `models.py`, роуты в `routers/`. Разбить `save_note` на мелкие функции.
> 4. **Про CSS:** Использовать CSS Variables (`:root`) и БЭМ-нейминг, группировать стили комментариями.

### 4. Принятые Решения (Decisions Log)

1.  **Обновление Правил Проекта:**
    В файлы `PROMPT.md` и `AI_SETTINGS.md` добавлен блок **Project Specific Rules**:
    *   **Persistence First:** Запрет на использование локальных JSON для важных данных в проде.
    *   **No Magic Numbers:** Обязательное использование именованных констант для колонок.
    *   **Error Visibility:** Обязательный вывод ошибок API пользователю (alert/toast).
    *   **Deployment Awareness:** Критическая проверка импортов в `api_server.py` перед пушем.

2.  **Оптимизация CSS:**
    В правила добавлен пункт:
    *   **CSS Optimization:** Использовать CSS Variables, группировку и плоскую структуру классов.

3.  **Документирование:**
    Создан этот файл (`CODE_REVIEW_HISTORY.md`) для фиксации всех этапов ревью и принятых архитектурных решений.

---
*Следующие решения будут добавлены ниже.*
